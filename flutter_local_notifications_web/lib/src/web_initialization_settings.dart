/// Web-specific initialization settings for the flutter_local_notifications
/// plugin.
///
/// This class is currently a placeholder that follows the federated plugin
/// pattern established by other platforms (e.g., `AndroidInitializationSettings`,
/// `DarwinInitializationSettings`). Future versions may add web-specific
/// configuration options such as custom service worker paths or notification
/// permission request timing.
class WebInitializationSettings {
  /// Creates a new instance of [WebInitializationSettings].
  const WebInitializationSettings();
}
